# `mcx_poker/__init__.py` 人话讲解

源文件：`/Users/machengqi/Documents/mcx-poker/src/mcx_poker/__init__.py`

## 这个文件是干什么的

源码说明：mcx-poker platform package.

人话概括：

这是包入口或导出文件，用来整理这个 Python 包对外暴露的名字。

## 初学者阅读建议

先看“这个文件是干什么的”，再看类和函数标题。遇到以下划线开头的函数，可以先理解成内部工具；遇到 `to_dict/from_dict/to_json/from_json`，重点记住它们是在对象、字典和 JSON 之间转换。
